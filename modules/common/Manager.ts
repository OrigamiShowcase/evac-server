import ConnectionManager from "../game/services/ConnectionManager";

export default class Manager
{
    static evac:ConnectionManager=new ConnectionManager()
    static uno:ConnectionManager=new ConnectionManager()
}